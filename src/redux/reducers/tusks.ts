const initialState = {
  queue: [],
  developnemt: [],
  done: [],
};

const tusker = (state = initialState, { type, payload }: any) => {
  switch (type) {
    default:
      return state;
  }
};

export default tusker;
